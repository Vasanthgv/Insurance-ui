version: "3.8"

services:
  mysql:
    image: mysql:8
    container_name: mysql-Insurence_db
    environment:
      MYSQL_DATABASE: policydb
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
   